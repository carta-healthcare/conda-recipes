package:
  name: parquet-python
  version: !!str 1.0.0

source:
  git_tag: master
  git_url: https://github.com/jcrobak/parquet-python.git

requirements:
  build:
    - python
    - python-snappy
    - thrift
    - setuptools

  run:
    - python
    - python-snappy
    - thrift

test:
  imports:
    - parquet

